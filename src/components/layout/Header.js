import React from 'react';

// Styled components
import { WebTitle } from '../styles/Title';

function Header() {
  return (
    <header>
      <WebTitle>Taking Note App</WebTitle>
    </header>
  )
}

export default Header;